const $beginScope = 'beginScope';
const $endScope = 'endScope';

/// key for value of type `Map<int, bool>`
const $emit = '_emit';

/// key for value of type `bool`
const $multi = 'multi';

const $wrap = 'wrap';

const kDefaultKeywordScope = 'keyword';
