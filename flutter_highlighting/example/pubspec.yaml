name: flutter_highlight_gallery
description: A new Flutter project.
publish_to: none

version: 1.0.0+1

environment:
  sdk: ">=2.17.0 <3.0.0"
  flutter: ">=3.0.0"

dependencies:
  flutter: { sdk: flutter }
  flutter_highlighting: { path: ../ }

flutter:
  uses-material-design: true

dependency_overrides:
  highlighting: { path: ../../highlighting }
